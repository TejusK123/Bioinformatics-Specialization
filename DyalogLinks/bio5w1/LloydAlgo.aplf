 R←k LloydAlgo Data;Centers;mat;Euclidean_Distance;ChooseCenters
 Euclidean_Distance←{*∘0.5+/⍺*∘2⍤-⍵}
 ChooseCenters←{{/∘⍳∘≢⍨⍵}¨((=∘(⌊/)⍨)¨⊂[1]⍵)}
 Centers←k↑Data

 :For i :In ⍳500
     mat←Centers∘.Euclidean_Distance Data
     Centers←{((+⌿)÷⊃∘⍴)↑⍵}¨{Data/⍨,↑(ChooseCenters mat)=⍵}¨⍳k
 :EndFor

 R←Centers
